server:
  port: 8084

spring:
  application:
    name: auth-service
datasource:
    url: jdbc:mysql://localhost:3307/ems
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  security:
    user:
      name: disabled
      password: disabled

eureka:
  client:
    service-url:
      defaultZone: http://${EUREKA_HOST:service-registry}:8761/eureka/
